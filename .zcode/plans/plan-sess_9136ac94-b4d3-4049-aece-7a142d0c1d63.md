## Deliverable

Create **`ExecutionPlan.md`** at the project root — the operational companion to Phases.md (which stays the strategic roadmap; ExecutionPlan.md adds the who/what/when/order-of-work). Style matches the existing doc set (same header format, tables, `---` separators). Also update **`memory.md`** §5 (Next Immediate Steps) and §2 to point at the new doc, per its own "update every session" rule.

## Document outline & key content

**1. Prerequisites (Phase 0 exit checklist)** — permission-matrix resolution (the PRD §10 blocker for RLS work), git repo + branch protection, Supabase dev project, Vercel connection, team role assignment.

**2. Delivery process (global rules)** — 2-week sprints; squash-merge per Rules.md §2; CI gates on every PR (format + analyze + unit + RLS contract tests); **both-platform rule**: no milestone is "done" until verified on latest Chrome AND Android emulator/device (your platform answer, baked into every DoD).

**3. Phase 1 (Months 1–3) — sprint-by-sprint, the most detailed section:**
- **Sprint 0 (Wk 0):** Supabase project + Auth enabled; Flutter scaffold with feature-based folders (`lib/core/`, `lib/features/...`) per Rules.md §1; pinned deps (`supabase_flutter`, `flutter_riverpod`, `go_router`); Design.md dark theme tokens; GitHub Actions CI; Vercel preview deploys.
- **Sprint 1:** Auth screens + `profiles` table + session state via Riverpod; typed-error pattern established.
- **Sprint 2:** Full Postgres schema (all Architecture.md §5 tables) as migrations; append-only `audit_log` enforced at grant level; pgTAP contract-test harness wired into CI.
- **Sprint 3:** Room creation, server-side hashed access-code generation, join flow, owner approval, code rotation, join rate-limiting + RLS policies with allow/deny tests.
- **Sprint 4:** Evidence vault (Storage bucket, 50MB limit, file hashing, duplicate-name versioning) + trigger-based audit writes.
- **Sprint 5:** Timeline, realtime discussion with @mentions, tasks CRUD; responsive layouts (mobile bottom-tabs / desktop sidebar).
- **Sprint 6:** Legal + Academic case-type configs (as data, not code); a11y + performance passes.
- **Sprint 7:** Hardening, demo-day flow (Phases.md §7 steps 1–3 + populated audit log) verified on both platforms, retro, Phase 2 Go/No-Go.

**4. Phase 2 (Months 3–5)** — Corporate/Medical/Technical configs (validates "config not rebuild"), field-level redaction + tests, notification feed, PDF/Word export (sanitized, no privileged leakage), entity-relationship map v1. Sprint-level, medium granularity.

**5. Phase 3 (Months 5–7)** — AI Adapter Layer with ONE provider wired end-to-end first (per Architecture.md §13 mitigation), workflow builder, 3 first agents, accept/edit/dismiss human-in-the-loop flow, prompt-injection hardening.

**6. Phase 4 (Months 7–12)** — marketplace, cross-case search, offline-first (conflict-resolution policy defined BEFORE building, per Phases.md risk note), store submission with early developer-account provisioning, billing groundwork. Quarterly checkpoints.

**7. Cross-cutting workstreams** — continuous: RLS contract tests gate every merge; component library built alongside features (Design.md §6); memory.md updated each session; security checklist per Rules.md §12.

**8. Milestone/gate summary table** — Phase exits with dates and Go/No-Go criteria (all lifted from Phases.md DoD sections so the two docs never contradict).

**9. Risk register** — condensed from Architecture.md §13 + Phases.md, each with owner and mitigation.

**10. Immediate next actions** — the concrete first week: starter permission matrix, repo/CI/Vercel/Supabase setup, scaffold, update memory.md.

## Notes

- I will NOT invent schema/API details beyond what Architecture.md defines (per Rules.md §11); anything the docs leave open (e.g., router choice, test tooling preference) is flagged in the doc as an explicit decision point rather than silently decided.
- No code changes in this task — planning docs only (memory.md edit is a two-line pointer update).